# Content Protect production operations runbook

Status: operational baseline. The first encrypted database backup, isolated restore and production-monitor alert destination have been verified; signed UK launch governance, external provider approvals, live-mode tests, retention approval and independent audit-export configuration remain release gates.

## Service-level targets

- Public application availability target: 99.9% monthly after commercial launch.
- Liveness check: `GET /api/health/live` every minute.
- Readiness check: `GET /api/health/ready` every minute.
- Alert when readiness fails twice consecutively, the 5xx rate exceeds 2% for five minutes, or p95 response time exceeds two seconds for ten minutes.
- SEV-1 acknowledgement target: 15 minutes. SEV-2: 60 minutes. SEV-3: next business day.

The readiness endpoint checks PostgreSQL, the latest required migration, the complete audit-integrity chain, private storage and external key configuration without writing customer data. It returns HTTP 503 and `status: degraded` when any of those infrastructure checks fails; local JSON or local disk can never pass the production readiness probe. `productionReady` is deliberately false until approved commercial image scanning, approved video-frame scanning, live Yoti age verification, live operator-reviewed delivery, live Stripe billing, successful recent retention evidence, successful recent external-monitor evidence, recent restore evidence, a recent independently retained audit export and a current signed UK launch-governance manifest are present. A scanner with an API key but missing privacy, transfer, adult-content or video-frame approval remains compliance-blocked and cannot open a production gate. Test or sandbox credentials do not satisfy a live operational gate, and manual boolean switches do not satisfy evidence-based gates.

The UK governance gate is defined in `docs/compliance/UK-LAUNCH-GOVERNANCE.md`. It uses an offline Ed25519 private key; the production service receives only the public key and signed compact manifest. The signature covers the exact migration, all current compliance versions, the approval period and the exact ten-control evidence set. Missing, altered, expired or stale-policy manifests fail closed. Never store the private key in Render, GitHub, this repository or ordinary operator devices, and never sign placeholder approvals merely to turn the health check green.

Yoti age assurance uses the official signed Digital Identity SDK in live mode. Configure both `YOTI_SDK_ID` and the PEM private key in `YOTI_PRIVATE_KEY`; an API key is not a substitute. The policy requests only the derived `age_over:18` attribute and rejects self-asserted evidence. Missing or invalid credentials disable the live age-check flow without making the application unavailable. Keep `YOTI_MODE=sandbox` until Yoti approves the organisation/application and a real end-to-end receipt test passes; only the separately approved `YOTI_MODE=live` can satisfy the production gate.

The Digital ID sandbox does not perform a real phone/app identity share. Content Protect therefore never sends a Sandbox SDK ID to the production Share API and never presents the live Yoti QR as if it were a working sandbox check. Instead, a controlled application-gating test is available only when both `YOTI_SANDBOX_TEST_EMAILS` and an opaque `YOTI_SANDBOX_TEST_APPROVAL_REFERENCE` are configured. The exact allowlisted account must be email-verified and re-enter its password. The result is stored as `testOnly` with `mode: sandbox`, contains no document, selfie or date of birth, and is automatically ignored by every protected feature after `YOTI_MODE` changes to `live`. This test proves Content Protect's access gates only; it is not evidence that Yoti verified the user and cannot open the production readiness gate.

Authentication, password reset, verification, MFA, sensitive downloads, exports and operator actions use database-backed rate limits in production. Limit keys are HMAC-SHA-256 pseudonyms derived with the external master key, shared by every application instance and expired by the retention job. Login protection applies both an account limit and a broader IP limit, so distributed password attacks and shared networks are handled independently. An application restart must never clear an attacker's accumulated attempts.

Monitoring, retention and backup-restore readiness are derived from fresh PostgreSQL evidence and have no manual override. A successful isolated restore reports through `/api/operations/backup-restore-evidence` using a dedicated `BACKUP_RESTORE_EVIDENCE_TOKEN`; readiness expires that evidence after 100 days and invalidates it immediately when the required schema migration changes. Executing retention evidence is also bound to the exact required migration and is invalidated by every schema change; preview runs never write successful evidence. Restore comparison covers every durable customer, case, consent, accounting, audit, operational-evidence and deletion-tombstone table. Set `RETENTION_EXECUTION_ENABLED=true` only inside the approved executing cron job after its production preview and policy are approved. Configuration values are never substitutes for completing the underlying control.

### External production monitor

The repository workflow `.github/workflows/production-monitor.yml` checks the public domain from infrastructure independent of Render every five minutes and after every push to `main`. Push-triggered runs first wait up to six minutes for the matching 12-character release identifier to become live, preventing a successful check of the previous deployment from being recorded for a new revision. Monitoring evidence is accepted only when its 12-character release identifier exactly equals the version currently served; every deploy therefore closes the monitoring gate until that exact release passes. To avoid a circular dependency, the external verifier may bootstrap the new heartbeat only when `monitoring` is the sole false operational gate and signed launch governance is approved; any second false gate fails the run and no heartbeat is written. It validates liveness, PostgreSQL readiness, private object storage, the required migration, TLS-facing security headers, legal pages and SEO. Before approval it may monitor the deliberately incomplete pre-launch service; as soon as the signed UK launch-governance status becomes `approved`, the verifier automatically requires `productionReady: true` plus every live commercial integration, including approved image and video scanning. This automatic transition cannot be disabled by leaving the workflow's pre-launch environment flag false. A single failed run is retried after 60 seconds so an alert represents two consecutive failures.

To commission the alert route, store the same random `MONITORING_HEARTBEAT_TOKEN` as a Render secret and a GitHub Actions repository secret. Open **GitHub → Actions → Production monitor → Run workflow**, enable **Fail after the checks to test alert delivery**, and run it. Confirm that the named on-call recipient receives the failed-workflow notification, then run it again without the failure option and confirm a green result. Only the successful run writes fresh monitoring evidence; GitHub notification settings must keep Actions failure notifications enabled for the on-call account.

### Production-monitor alert delivery evidence

The alert route was acceptance-tested on 19 July 2026. GitHub Actions **Production monitor #27**, run ID `29704210377`, was deliberately failed at 22:21 BST using the workflow's test-failure option. The director on-call mailbox received GitHub's failure notification from `notifications@github.com` at 22:22 BST with subject metadata binding it to `Production monitor`, branch `main` and commit `5783aaf`. The mailbox search result was checked in the authenticated account; no personal mailbox address or message body is retained in this repository. Manual recovery **Production monitor #28**, run ID `29704238669`, completed successfully at 22:22 BST. This proves both failure delivery and recovery. Re-test after changing the on-call mailbox, GitHub notification settings or workflow ownership.

Real takedown delivery also remains disabled until specialist counsel approves the exact notice template. After approval, record the approved version in Render as `TAKEDOWN_LEGAL_APPROVED_VERSION=2026-07-19-v3`. Keep `TAKEDOWNS_MODE=sandbox` during preparation and testing; the dispatch endpoint refuses all external delivery until a separately approved change sets `TAKEDOWNS_MODE=live`. Never advance either value merely to make the readiness check green; a template change requires a new review and version.

Operator access requires three separately managed Render secrets: a random `TAKEDOWN_OPERATOR_TOKEN` of at least 32 characters, an internal nominal reference in `TAKEDOWN_OPERATOR_ID`, and a unique 160-bit Base32 `TAKEDOWN_OPERATOR_TOTP_SECRET` enrolled in the named operator's authenticator. The raw token can only be exchanged together with a current TOTP code for a one-hour HttpOnly, Secure, SameSite=Strict session; bearer-token API access is not accepted. Every irreversible notice dispatch requires a fresh TOTP step-up, and a code cannot be reused within its validity window. Operator login, preparation, failure, delivery and logout actions use the operator's distinct pseudonymous actor in the tamper-evident audit chain. Keep the recovery material in the company's approved password manager, never in source code, chat, tickets or ordinary email.

Every uploaded reference file must carry declaration version `2026-07-19-v1`, recording the claimant's capacity, rights-holder legal/business name, a short restricted evidence reference and explicit authority/accuracy confirmations. A creator may replace the declaration only before a takedown case preserves it. Case creation freezes the declaration inside the evidence hash. Before preparing a notice, the operator must review the separately restricted source file, original post, licence or agency record and store only its opaque reference in Content Protect. That review moves the declaration to `verified`; it is not identity verification. The exact notice exposes the legal claimant name, professional name where present, reviewed rights holder and claimant capacity, so the creator must review that disclosure before approving the immutable notice hash.

Every takedown case also requires a current creator-supplied JPEG, PNG or WebP capture of the matched public page. The capture is a separate application-encrypted evidence object, does not consume a plan reference-file slot, is never scanned, is included in encrypted media backups and is bound to the match URL, host, server capture time and case through its SHA-256 checksum. Replacement is allowed only before case creation. The creator can retrieve it only after password reauthentication; an operator needs an authenticated one-hour session, a current non-reusable TOTP code and an explicit need-to-review confirmation. The operator must compare it with the live URL and confirm review before preparing the notice. A creator-supplied capture is evidence, not independent notarisation or proof that the use is unlawful.

### Dispute intake and freeze

The public `/disputes.html` form is the safe text-only intake channel. A valid intake must match both a delivered case reference and the exact HTTPS target URL, but the endpoint returns the same generic acknowledgement for an unknown/non-delivered case to prevent enumeration. Complete contact and statement fields are AES-256-GCM encrypted with the application key. Only a keyed contact pseudonym, category, country and statement checksum remain in ordinary case metadata. Each received/review event is HMAC-bound to its case, timestamp and exact encrypted details, and the integrity hash is copied into the tamper-evident audit chain; logs and audit events contain no contact email or statement. A matching intake immediately clears `nextActionAt` and moves the case to `Disputed — review required`.

Operators see metadata only until a current non-reusable TOTP step-up decrypts a specific open dispute; every access is audited. A different current code is required to record the outcome. Escalation remains open and frozen. Acceptance closes the case. Continuation is rejected unless a qualified-counsel record reference and creator-notification confirmation are supplied. Multiple open disputes keep the case frozen until all are resolved, and any accepted dispute produces final closure. Never request identity documents or intimate attachments through this form or ordinary email. Before real notices, counsel and the named operator must acceptance-test valid, invalid, duplicate, escalated, accepted and counsel-approved continuation paths.

### TinEye image and video-frame scanning activation

The public TinEye sandbox was exercised on 19 July 2026 with a generated image and the deployed request format: HTTPS `POST /rest/search/`, `x-api-key`, metadata-stripped JPEG upload, result/backlink parsing and the 5% `query_match_percent` filter all passed. The sandbox returns fixed demonstration matches and is not evidence of production coverage. The application pins image transmission to `https://api.tineye.com/rest/search/` to prevent accidental disclosure to a substituted endpoint.

Do not purchase or install a production key until `docs/vendor-due-diligence/TINEYE-ACTIVATION.md` is complete. A key alone leaves the scanner in `compliance-blocked`. After the underlying evidence is genuinely approved, store its internal legal/privacy record identifier in `TINEYE_DATA_PROTECTION_APPROVAL_REFERENCE`, store TinEye's written acceptance reference for lawful explicit-adult creator media in `TINEYE_ADULT_CONTENT_APPROVAL_REFERENCE`, and only then add `TINEYE_API_KEY`. Never put approval text, emails or personal data in environment values—use opaque record or ticket references. Confirm `/api/health/ready` reports `scanner: tineye-commercial`, then perform one consented non-explicit production test before any intimate reference is submitted.

Video support is implemented but has a stricter independent gate. The application accepts MP4, MOV and WebM files up to 8 MB and rejects videos longer than ten minutes during frame extraction. It runs `ffprobe` and `ffmpeg` in the application container with network protocols restricted to the input pipe, a single worker thread, bounded execution time/output and no plaintext temporary file. It extracts at most three resized JPEG frames in memory and excludes audio, subtitles, data streams and metadata; the full video is never sent to TinEye. Every frame still passes through the normal resize, metadata removal, endpoint pinning and result filtering path. Duplicate public URLs returned for multiple frames are collapsed while the per-frame evidence is retained.

Do not set `TINEYE_VIDEO_FRAME_APPROVAL_REFERENCE` until TinEye's written adult-content permission expressly covers derived video frames, the DPIA and international-transfer review cover this flow, counsel has approved and published updated customer-facing Privacy Notice and Service Terms versions, and the up-to-three-queries-per-video cost has been approved. Without this opaque evidence reference, `/api/health/ready` reports `videoScanning: privacy-blocked`; videos remain stored only and no frame extraction or provider request occurs. After activation, perform a non-explicit, consented video test first and verify exactly one to three provider requests, deduplicated results, audit evidence and deletion.

## Logs and correlation

The service emits one JSON event per request with UTC platform timestamp, request ID, method, path, status and duration. Error events contain the same request ID. Query Render logs using `requestId`; do not log request bodies, cookies, tokens, email addresses or private media.

Keep application/security logs for 12 months, with access restricted to authorised operators. Export logs to a separate retained destination before relying on them for incident evidence.

PostgreSQL audit events are append-only at the database layer and form a versioned HMAC-SHA-256 chain derived from `CONTENT_PROTECT_MASTER_KEY`. Startup migrates all legacy events atomically under an advisory lock; readiness independently verifies every retained event and the continuity between consecutive sequence numbers. A changed action, timestamp, detail object, actor pseudonym, sequence or link fails readiness. Account deletion can null the relational `user_id` without changing the immutable pseudonymous actor hash. Only the retention transaction may delete expired audit rows; a retained suffix remains individually verifiable and chain-contiguous. Never rotate the master key without a reviewed audit-chain re-signing migration and retained evidence of the former chain head.

The database chain provides tamper evidence, not an independent custody boundary. The `pnpm audit:export` job now reads the complete verified chain directly from PostgreSQL, removes relational user identifiers, serialises only pseudonymous audit fields, compresses and AES-256-GCM encrypts the archive, and writes a signed manifest plus archive under a unique daily key with `If-None-Match: *`. It then reads both objects back, validates the HMAC signature, authenticated encryption, checksum, sequence continuity, chain boundaries and a real 400-day bucket lifecycle before recording successful PostgreSQL operational evidence. A failure records failed evidence when possible and fails the scheduler; readiness accepts success for only 36 hours and binds it to the required migration.

Run this job only in a separate scheduled service that has `DATABASE_URL`, `CONTENT_PROTECT_MASTER_KEY`, `AUDIT_EXPORT_OBJECT_STORAGE_ENDPOINT`, `AUDIT_EXPORT_OBJECT_STORAGE_BUCKET`, `AUDIT_EXPORT_OBJECT_STORAGE_REGION`, `AUDIT_EXPORT_OBJECT_STORAGE_ACCESS_KEY_ID`, `AUDIT_EXPORT_OBJECT_STORAGE_SECRET_ACCESS_KEY`, `AUDIT_EXPORT_ENCRYPTION_KEY`, `AUDIT_EXPORT_EVIDENCE_KEY` and an opaque approved `AUDIT_EXPORT_CUSTODY_REFERENCE`. The destination bucket and credentials must differ from both primary media and backup storage. Grant read/write/list/lifecycle-read but no delete permission to the job; the web service must receive none of these audit-export credentials. Keep encryption and evidence keys in the managed secret store and an approved recovery escrow. This implementation does not make an unselected provider compliant: approve its DPA, transfer mechanism, subprocessor terms and separate administrative custody before configuration.

Application platform logs still require a compatible redacted log drain or SIEM. Do not export request bodies, cookies, tokens, email addresses or media. Test the alert raised by an audit-integrity readiness failure and retain proof that the named on-call recipient received it.

## Daily checks

1. Run `pnpm run verify:production` against the primary domain.
2. Review readiness, 5xx count, latency and restart events.
3. Review failed Stripe, Resend, scanner and age-provider webhooks.
4. Review the operator case queue and overdue actions without opening private media unnecessarily.
5. Run `pnpm retention:preview` and investigate unexpected volumes. The destructive command remains disabled unless the approved scheduler supplies `RETENTION_EXECUTION_ENABLED=true`; never enable it before migrations and a reviewed preview.
6. The Blueprint initially defines `content-protect-retention` at 03:17 UTC daily in preview-only mode. It receives `DATABASE_URL` from the web service through Render's service reference. Review a real production preview before changing its command to `node scripts/retention.mjs --execute`, supplying the same private-object-storage settings as the web service and setting `RETENTION_EXECUTION_ENABLED=true` in a separately approved release. Execution first persists every eligible object key in PostgreSQL, commits the related lifecycle deletion, and then drains the idempotent storage-deletion queue in bounded batches. Failed objects remain queued for retry and the job records failed evidence. A successful job records evidence only after the queue is empty. Readiness uses the latest result, accepts success for less than 36 hours, and therefore closes immediately after a failed run; previews never open the gate.
7. Confirm that no provider credentials or customer data appeared in logs.

### Production retention preview evidence

After migration `022_subscription_consent_binding.sql`, the director manually triggered Render cron job `content-protect-retention` on 20 July 2026 at 03:39 BST against deployed revision `41c375aa4c1b8d38d1f855e5ba1be596f9e6ff5c`. Render showed the exact command `node scripts/retention.mjs`, without `--execute`. The application evaluated PostgreSQL at `2026-07-20T02:39:40.929Z` with `execute: false`; the cron run finished successfully at 03:39:44 BST. It reported zero object-deletion candidates, expired assets, sessions, operator sessions, tokens, failed verifications, orphan evidence captures, old audit events, webhook receipts, operational evidence, closed cases, closed consumer cases, orphan matches/scans, unverified accounts, expired accounting records and closed security incidents. It identified 37 expired rate-limit entries only. Because preview mode rolls back the transaction and never drains the object-deletion queue, no database row or storage object was deleted.

This evidence proves that the preview path runs successfully against production data and that its current candidate volume has been reviewed. It does not approve destructive retention, does not open the readiness retention gate and must not be used as a substitute for the approved policy, counsel sign-off, storage-deletion dry run and separately authorised executing release.

## External monitoring evidence

The GitHub production monitor runs public production and SEO checks every five minutes. After both checks succeed, it calls the machine-only heartbeat endpoint with `MONITORING_HEARTBEAT_TOKEN`; the same random value must be stored as a Render environment secret and a GitHub Actions repository secret. The endpoint applies constant-time credential comparison, rate limiting and server-side timestamps. Readiness accepts only successful evidence less than 15 minutes old. Never place the token in source code or workflow logs.

Stripe access is reconciled from the current Subscription object for checkout, subscription and invoice events; an invoice event alone must never grant or revoke access from its historical snapshot. The webhook destination must subscribe to checkout completion, subscription created/updated/deleted/paused/resumed, invoice paid, payment failed and payment action required. Checkout creation uses a 30-minute per-user/plan idempotency window to prevent duplicate subscriptions during retries.

Migration `022_subscription_consent_binding.sql` deliberately marks every pre-existing Stripe subscription without a cryptographically opaque retained billing-consent identifier as `unverified`; it does not guess a consent from the same customer or plan. Entitlement requires the subscription's exact account, plan, configured price, test/live mode, consent identifier and Service Terms version to match the retained consent and originating completed Checkout session. Later subscription and invoice events may update access only after that exact binding already exists. A mismatched event revokes local entitlement but can never silently start another checkout: any non-terminal Stripe subscription identifier blocks creation of a second subscription and directs the customer to the billing portal or support.

Account deletion retrieves the exact Stripe subscription, verifies both its subscription and customer identifiers and its test/live mode, and ends recurring billing before personal data removal. Immediate cancellation explicitly disables a final invoice and proration. After Stripe confirms `canceled` or `incomplete_expired`, the application persists that ended state before checking all pending customer invoice items and all open/draft invoices for the exact subscription. Account removal proceeds only when the complete pending-item list is empty and every remaining open/draft invoice is mode/customer/subscription-bound with automatic advancement paused. If this second check fails, recurring billing remains ended but the account and customer binding are retained for support review. Before live activation, confirm the Stripe account has no metered prices or manually created pending invoice items in this product, test deletion with a Stripe test subscription and retain the non-personal event and audit references. Stripe documents that manually created pending invoice items require separate review even after cancellation; never create or collect one automatically as part of account deletion.

Keep `PAYMENTS_MODE=test` through checkout, webhook, cancellation, failed-payment and customer-portal acceptance tests. Only production keys, production price IDs and a separately approved `PAYMENTS_MODE=live` change can satisfy the billing release gate.

### Verified customer refunds

Customer billing, cooling-off and refund requests are handled in the encrypted **Customer requests** queue. The operator records a reasoned approval or decline first; an approval stores the exact GBP amount and an opaque retained decision reference. A completion can no longer be entered manually. The operator must then open the restricted case, select **Execute or reconcile Stripe refund**, enter the original `pi_` payment-intent reference and use a fresh non-reusable TOTP step-up.

An account with an open customer request cannot be deleted until the request is closed, preventing loss of the customer/payment binding during investigation. A closed request with an approved or partial refund also continues to block deletion until Stripe verifies the refund as `succeeded`; this includes historical `legacy-recorded` completions. Only after that provider verification and case closure can account deletion detach the retained support record from the active account while the encrypted complaint record remains restricted for the 24-month period disclosed in the Privacy Notice.

The password-confirmed account data export includes the customer's support-case submission and the decrypted event timeline belonging to that account, including customer messages, decisions and provider-status evidence. The ordinary support list remains minimised and does not expose restricted event bodies. Treat the exported JSON as sensitive personal data and never attach it to ordinary support email.

Before creating a refund, the server retrieves Stripe's PaymentIntent with its latest charge and the uniquely associated paid InvoicePayment with its expanded subscription invoice. It fails closed unless the payment is succeeded, GBP, in the configured test/live mode, owned by the same Stripe customer, and has enough unrefunded balance. The paid invoice must also carry the Content Protect account, mode and configured `price_` metadata created during checkout; matching the customer alone is not sufficient. The approved amount is immutable at execution. Stripe receives an idempotency key bound to the case, retained decision event and numbered attempt; retries cannot create duplicate refunds. Pending and action-required results remain unresolved and are reconciled from Stripe. Failed or canceled results require a new explicit operator confirmation before another numbered attempt. Only a provider-returned `re_` result with status `succeeded`, matching payment intent, invoice binding, currency and amount can complete the refund and allow case resolution. Every submission/status change is encrypted in the append-only case timeline and minimised in the tamper-evident audit chain.

Migration `021_verified_stripe_refunds.sql` preserves any historical `refund-completed` event as immutable legacy evidence but the application rejects creation of new manual completion events. A historical billing/refund case that the earlier workflow allowed to reach `resolved` or `closed` while its refund decision was still `pending` is reopened as `in-review`, with its resolution/closure timestamps cleared; the migration never invents a decline or approval. PostgreSQL also rejects impossible combinations between the refund decision, approved amount, provider status, provider references and completion timestamp; `succeeded` cannot exist without a completion timestamp and an unsubmitted refund cannot carry provider identifiers. A historical completion is labelled `legacy-recorded`; it must not be represented as provider-verified. The operator must provide both the historical `re_` refund and `pi_` payment references so the application can retrieve and validate the existing Stripe refund without creating another one.

Run the complete flow in Stripe test mode using a real test subscription and test PaymentIntent before live activation. Confirm a wrong customer, live/test mismatch, non-GBP payment, over-refund and repeated request all fail without creating a second refund. Do not paste card details, bank details or customer correspondence into the case or Render logs.

## Database backup and restore gate

1. Use a paid managed PostgreSQL plan with point-in-time recovery enabled.
2. Create a separate AES-256-GCM encrypted logical backup on a daily schedule; keep 35 daily and 12 monthly recovery points.
3. Restrict backup access to the director and named technical operator, with MFA.
4. Every quarter, restore the newest backup into an isolated empty database.
5. Run all migrations, compare counts and sample integrity hashes across every durable customer, case, consent, accounting, audit, operational-evidence and deletion-tombstone table.
6. Record restore start/end times, recovery point, evidence, discrepancies and approver. Delete the restore environment securely.

A backup is not considered operational until one restore has succeeded. Never download production dumps to an unmanaged personal device.

Create the encrypted PostgreSQL custom-format archive and its non-personal signed integrity manifest with `pnpm backup:database`. The job exports one repeatable-read PostgreSQL snapshot, uses that same snapshot for `pg_dump` and the keyed integrity samples, encrypts the dump with a separately managed `BACKUP_ARCHIVE_ENCRYPTION_KEY`, uploads the ciphertext first and publishes `manifest.json` only after the archive succeeds. Neither database credentials nor encryption keys may appear in command arguments or logs. The legacy `pnpm backup:manifest` command creates evidence only and is not itself a database backup.

Restore with `pnpm backup:restore-database <manifest-key>` into a separately provisioned empty PostgreSQL database. The command refuses the source database and non-empty targets, validates the manifest signature, ciphertext size/checksum and AES-GCM authentication before invoking `pg_restore`, then compares counts plus HMAC integrity samples across all durable service and deletion-control tables. A discrepancy, altered archive or wrong key exits non-zero. When `BACKUP_RESTORE_EVIDENCE_URL` and its dedicated token are configured, only a completely successful restore reports authenticated evidence to production; partial configuration fails closed. Evidence is bound to the required migration, so a schema change requires a new backup and isolated restore before the gate can reopen. The older `pnpm backup:verify-restore` command verifies an already-restored database against a local manifest but does not perform restoration. Backup recoverability and retention are independent release gates: an isolated restore must be testable before destructive lifecycle expiry is approved, while the retention gate remains closed until its own lifecycle and deletion evidence exists.

The scheduled backup job performs the drill inside its own ephemeral container. After creating the newest encrypted archive it starts a PostgreSQL cluster bound only to `127.0.0.1`, restores and verifies the archive, reports authenticated evidence, stops PostgreSQL and deletes the temporary cluster. It runs whenever current restore evidence is missing or invalid, on the first UTC day of each calendar quarter even when evidence is still fresh, or when an operator explicitly sets `BACKUP_FORCE_RESTORE_DRILL=true` for a run. The restore child never receives `DATABASE_URL`, and the CI image check requires `initdb`, `pg_ctl`, `createdb` and `pg_restore` to exist. This design does not require another managed database and never downloads a production dump to an operator device.

### Latest schema-bound restore evidence

After migration `019_incident_register.sql`, the prior restore evidence was correctly invalidated because it covered schema `018`. The director manually triggered Render cron job `content-protect-backup` on 20 July 2026 at 00:04 BST. It created database snapshot `2026-07-19T23-04-14-207Z-f422ac14-c7f5-4cfe-aef8-d8d9ba151a22`, an AES-256-GCM encrypted archive of 78,702 bytes and a separate media snapshot containing one already-encrypted object. The job restored the database into isolated identity `5914d03f0d9f57c2`, compared all 17 durable tables—including `security_incidents` and `security_incident_events`—and reported zero discrepancies. Restore verification completed at `2026-07-19T23:04:21.911Z`; the authenticated evidence was accepted by production at `2026-07-19T23:04:22.055Z`, bound to release `0b18b2afa6a3b7496908363b123722ae3d8fe540` and required migration `019_incident_register.sql`. Render marked the cron run successful at 00:04:26 BST. `/api/health/ready` then reported `backupRestore: true` and `backupRestore: verified-recently`.

## Media storage recovery

Cloudflare R2 does not currently implement the S3 bucket-versioning operations, so R2 versioning must not be claimed as recovery evidence. Keep the primary R2 bucket private and create a second private backup bucket with separate credentials that are unavailable to the web service. Run `pnpm backup:media` from an isolated scheduled backup job with the primary read credentials, backup write credentials, PostgreSQL access and `BACKUP_EVIDENCE_KEY`. The job writes each already application-encrypted object under a unique daily or monthly snapshot prefix and publishes the signed manifest only after every object succeeds.

Configure backup-bucket lifecycle rules for the daily and monthly prefixes under both `content-protect-media/` and `content-protect-database/`, using 35 and 400 days respectively. The backup credentials used by the web application must not exist: only isolated backup/restore jobs may access backup archives, and the normal writer role should not delete them. Public bucket access must remain disabled.

Run `pnpm backup:verify-media <manifest-key>` using backup read credentials and the evidence key. This verifier needs no primary-bucket or database credentials, requires the enabled 35-day daily and 400-day monthly lifecycle rules (or the explicitly approved override values), validates the signed inventory, downloads every encrypted backup object and compares its size and SHA-256 digest. Retain the resulting JSON with the quarterly restore evidence. An absent object, altered manifest, lifecycle mismatch or checksum mismatch exits non-zero. A manifest is not valid recovery evidence until this independent read test succeeds.

Cloudflare capability reference: https://developers.cloudflare.com/r2/api/s3/api/ and lifecycle reference: https://developers.cloudflare.com/r2/buckets/object-lifecycles/

## Deployment and rollback

1. Deploy only a committed main-branch revision after the `Build and policy checks` workflow has passed. It installs the frozen lockfile, runs the offline policy/unit suite, builds the production assets and validates the final Docker image. Dependabot proposes grouped weekly dependency and GitHub Actions updates for review; updates are never deployed solely because they are available.
2. Run migrations through the Blueprint pre-deploy command. `schema_migrations` checksums must match the committed files, and `/api/health/ready` must report the latest required migration before traffic is declared healthy.
3. Run the production verifier and one creator sandbox journey.
4. Roll back the application revision if errors increase. Do not roll back a destructive schema change; use a reviewed forward fix.
5. Record revision, operator, outcome and any incident reference.

## Incident start

For suspected intimate-media, credential, identity or key exposure, declare SEV-1 and follow `docs/compliance/INCIDENT-RESPONSE-PLAN.md`. Revoke affected sessions and provider keys, preserve evidence, and record the UK breach-awareness time. Do not attach intimate media to ordinary support email.

Open `/operator`, select **Incident register**, then declare the event with named response roles and a fresh authenticator code. When a personal-data breach is identified, enter the actual company-awareness time; the application calculates and displays the 72-hour ICO deadline and preserves that awareness history even if the assessment later changes. Record containment, evidence preservation, processor escalation, recovery and communications as separate UTC timeline events. Do not copy raw intimate media, credentials, keys or identity documents into the register.

Notification decisions require a new authenticator code and a written rationale even when notification is not required. A required notification remains open until its completed time is recorded; an ICO completion also requires the submission reference. Closure is refused until recovery evidence, both notification decisions, root cause, corrective actions with owners/deadlines and an independent-review reference exist. PostgreSQL encrypts restricted incident details with the application master key, keeps event rows append-only and includes both incident tables in encrypted backup/restore verification. After any schema migration, create a new backup and successful isolated restore before treating the backup gate as current.
